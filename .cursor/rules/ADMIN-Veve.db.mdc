---
description: Veve DB {KEY} — leggi jira_issue, gap repo, groom jira_issue; WIP solo su gogo
alwaysApply: true
---

# Veve DB — `veve db {KEY}` e workflow database step 3

Variante **cruscotto SQLite**: stessa sequenza e **stessi template** di `ADMIN-Veve.mdc`, ma **lettura** da `jira_issue` (cache) e **scrittura groom** su **`jira_issue.raw_fields`** — **non** `jira_issue_wip` · **non** `editJiraIssue` / MCP Jira per description.

**WIP (`jira_issue_wip`):** solo ticket **in lavorazione** — iscrizione al **click gogo** (`enrollIssueInWip`); purge al **merge PR** confermato (step 9 / `purgeWipBundleIfCacheAligned`).

Sequenza agente: **Leggi DB** → **Analizza repo** → **Aggiorna story cache** → **Aggiorna subtask cache** (groom + crea mancanti se gap chiaro).

| Step veve db | Regola | Equivalente veve MCP |
| --- | --- | --- |
| 1 | `ADMIN-Veve.db-LeggiJira.mdc` | `ADMIN-Veve-LeggiJira.mdc` |
| 2 | `ADMIN-AnalizzaRepo.mdc` | `ADMIN-AnalizzaRepo.mdc` (identica) |
| 3 | `ADMIN-Veve.db-AggiornaStory.mdc` · `veve-story-parent.md` | `ADMIN-Veve-AggiornaStory.mdc` |
| 4 | `ADMIN-Veve.db-AggiornaSubtask.mdc` · `veve-subtask.md` | `ADMIN-Veve-AggiornaSubtask.mdc` |

## Trigger

| Contesto | Comportamento |
| --- | --- |
| `veve db {KEY}` | Standalone — chat libera, sequenza completa 1→4 |
| `procedi` / `gogo {KEY}` con `ADMIN-Workflow database.mdc` | **Step 3** workflow — silenzioso (`ADMIN-Silence.mdc`) |
| Matrix **CREA** | Post `createMatrixFindingIssue` → `runVeveDbForIssueKey` (default `jira_issue`) |
| `chiudi` | **Non** esegue veve db |

`{KEY}` = `{JIRA_KEY}-{num}` del progetto istanziato (`ADMIN-96`, `JLO-507`, …).

## Parità con veve MCP

| Aspetto | Veve MCP | Veve DB |
| --- | --- | --- |
| Template parent | `veve-story-parent.md` | **Stesso** |
| Template subtask | `veve-subtask.md` | **Stesso** |
| Stato repo in description | Tabella solo ✅/⚠️/❌ | **Stesso** |
| AC/DoD checkbox | Regola B poi A | **Stesso** (`ADMIN-Veve-AggiornaStory/Subtask.mdc`) |
| Crea subtask mancanti | Step 4 — implementazione + `[Test]` | Step 4 — `suggestVeveMissingSubtasks` |
| Dove scrive description | Jira Cloud (`editJiraIssue`) | **`raw_fields.veveDescription`** su **`jira_issue`** |
| Lettura ticket | MCP `getJiraIssue` | **`jira_issue`** ultimo sync |

**Unica eccezione Jira live in veve DB:** creazione subtask mancanti via REST (`createJiraIssue` in `jiraCORE.jira.create.mjs`) — poi `ensureJiraIssuesInCache` e groom su **`jira_issue`**.

## Prerequisito DB

| Controllo | Stop se |
| --- | --- |
| `cruscotto.db` | Assente o non migrato (`node cruscotto.database/migrate.mjs`) |
| Sync cache | Nessun `jira_issue` per `{KEY}` dopo `ensureJiraIssuesInCache` |
| Overlay | `CRUSCOTTO_DB_PATH` / `PRJ_NAME` non allineati |

Sync backlog: `npm run db:sync` (host PortalAdmin) prima del primo `veve db` su key assente in cache.

## Persistenza groomed

| Operazione | Tabella sorgente | Tabella destinazione groom |
| --- | --- | --- |
| Lettura parent/subtask | `jira_issue` (ultimo `sync_run` success) | — |
| `veve db` / gogo step 3 / Matrix CREA | `jira_issue` | **`jira_issue.raw_fields`** merge |
| **gogo** (click backlog) | `jira_issue` | **`jira_issue_wip`** — `enrollIssueInWip` (parent + subtask) |
| veve db **con parent già in WIP** | `jira_issue` groom | **`syncWipGroomFromJiraIssueCache`** — copia solo groom in WIP, preserva stato workflow |
| Step 5–7 / PUSH | `jira_issue_wip` | update WIP; step 8 sync → `jira_issue` |
| Merge PR (step 9) | `jira_issue` allineata | **delete** bundle da `jira_issue_wip` |

Campo JSON `raw_fields`: `{ "veveDescription": "…", "gapSummary": "…", "gap": {…}, "workflowSource": "veve-db", "updatedAt": "ISO" }`.

## Runtime programmatico (server / CI / Matrix)

| Modulo | `admin.portal.JiraCORE/jiraCORE.veve.db.mjs` |
| --- | --- |
| Export | `runVeveDbForIssueKey(key, opts)` |

Ordine interno runtime (dopo gap): **crea subtask mancanti** → **groom parent** → **groom ogni subtask** → **`syncWipGroomFromJiraIssueCache`** se parent in WIP.

Opzioni: `writeTarget` default **`jira_issue`** · `createSubtasks: false` · `dryRun: true` · `matrixPaths` · `matrixKind`.

## Ripresa

Se veve db già eseguito nella stessa sessione su `{KEY}` → salta step 3 workflow (`ADMIN-Workflow database.mdc`).

Se step 4 crea nuove subtask → **ripeti** step 3 (parent cache: tabella ordine subtask).

## Fallback

| Situazione | Azione |
| --- | --- |
| DB assente | `npm run db:migrate` |
| Key assente in cache | `npm run db:sync` o `veve {KEY}` MCP per ticket nuovo |
| Groom senza REST | Solo patch `jira_issue` — **non** `editJiraIssue` · **non** upsert WIP diretto |

Variante **MCP** (Jira live diretto): `ADMIN-Veve.mdc` · `veve {KEY}` — percorso alternativo standalone, **non** step 3 workflow database.

**Vietato:** `CallMcpTool` Jira per groom description · `editJiraIssue` per description · scrittura diretta `jira_issue_wip` da veve db · branch · commit · transizioni Fatto (salvo richiesta esplicita).
