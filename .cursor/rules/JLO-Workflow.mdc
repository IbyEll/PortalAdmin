---
description: Alias workflow — ADMIN-Workflow.mdc; JLO = progetto istanziato JustLastOne (PRJ_JIRA_PREFIX=JLO)
alwaysApply: true
---

# JLO-Workflow (progetto istanziato)

Regola canonica: **`.cursor/rules/ADMIN-Workflow.mdc`**.

| | |
| --- | --- |
| Overlay | `PRJ_NAME=JustLastOne` |
| Repo workflow | `PRODUCT_REPO_PATH` → checkout **JustLastOne** (non il solo host PortalAdmin) |
| `JIRA_KEY` | `JLO` (= `PRJ_JIRA_PREFIX`) |
| Branch | `STORY---JLO-{num}-{slug}` |

Comandi: `gogo JLO-507` · `chiudi fast Story JLO-930` — **stop** se istanza non allineata a JustLastOne.

Ticket **ADMIN**: stessa regola con overlay `AdminDashBoard` e repo workflow PortalAdmin.
