---
description: Analisi avanzamento PortalAdmin — architettura, gap, bug, feature. Script docs.portal/matrix.portal.gap.analysis.mjs
globs: docs.portal/matrix.portal.gap.*, docs.portal.lib/matrix.gap.*, docs.portal.lib/docs.portal.analysis.mjs
alwaysApply: false
---

# ADMIN — Avanzamento Gap Feature

## Trigger

| Comando / richiesta | Effetto |
| --- | --- |
| `analizza avanzamento` · `avanzamento gap feature` | Esegui analisi + rigenera HTML |
| Modifica architettura host significativa | Rigenerare pagina dopo implementazione |
| `node docs.portal/matrix.portal.gap.analysis.mjs` | CLI diretta |

## Artefatti

| Path | Ruolo |
| --- | --- |
| `docs.portal.lib/matrix.gap.scan.mjs` | Scansione finding + metriche |
| `docs.portal/matrix.portal.gap.analysis.mjs` | CLI analisi + render HTML/JSON/MD |
| `docs.portal/matrix.portal.gap.html` | Pagina unificata servita su HOME `/docs/` (avanzamento + audit + obsoleto) |
| `docs.portal.lib/matrix.unified.mjs` | Builder sezioni operativo + audit con dedup |
| `docs.portal.lib/docs.portal.analysis.mjs` | 15 controlli automatici base |

Alias legacy (redirect): `matrix.avanzamento.gap.feature.html`, `Avanzamento_Gap_Feature.html` → `matrix.portal.gap.html`.

## Sequenza agente

1. **Esegui** `node docs.portal/matrix.portal.gap.analysis.mjs` dalla root PortalAdmin.
2. **Leggi** metriche stdout (arch %, gap, bug).
3. **Apri** `docs.portal/matrix.portal.gap.html` — sezioni operativo (`op-*`) e audit (`audit-*`, anchor `#audit-parking`).
4. Se gap P0/P1 toccati dal task corrente → aggiorna finding o risolvilo nel codice, poi **rigenera**.

## Cosa analizza lo script

| Area | Metodo |
| --- | --- |
| Architettura target | existsSync + checklist migrazioni (overlay, dual server, JiraCORE, docs, smoke path file) |
| Gap | `analyzeRepository` + PARKING import grep |
| Bug | grep import mancanti (tree regenerate, Confluence, CI portal-paths, npm path) |
| Deprecation | runner/ comment drift, stub start-dev, portal-paths consumer |
| Feature / miglioramenti | catalogo curato in matrix.gap.scan.mjs |

## Categorie output HTML

`avanzamento` · `architettura` · `gap` · `bug` · `deprecation` · `feature` · `miglioramento`

Severità: `P0` · `P1` · `P2` · `info`

## Distinction vs Aggiorna (docs chrome)

| | **Aggiorna** (toolbar docs) | **matrix.portal.gap** |
| --- | --- | --- |
| Trigger | UI Documenti | CLI / regola / agente |
| Output | Sezione auto in ogni doc esistente | Pagina dedicata completa |
| Scope | Controlli + tabella per titolo pagina | Report trasversale categorizzato |

## Vietato

- Assumere path legacy (`scripts/`, `cruscotto.frontend/jira/`) senza verifica `existsSync`
- Edit manuale solo HTML senza rigenerare da script (drift garantito)
