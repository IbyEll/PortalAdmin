---
description: Veve MCP step 4 — aggiorna subtask (standalone `veve {KEY}` only)
alwaysApply: false
---

# ADMIN Veve — Aggiorna Subtask

## Trigger

`aggiorna subtask ADMIN-xxx` · `veve ADMIN-xxx` step 4

## Per ogni subtask

Template: `.cursor/templates/veve/veve-subtask.md`

`editJiraIssue`: Obiettivo, Stato repo, AC/DoD, file coinvolti, ordine N/M — compila placeholder da gap analysis.

Sezioni e **checkbox AC/DoD (B poi A)**: vedi `JLO-Veve-AggiornaSubtask.mdc` (canonico).

**Vietato:** branch, commit, Fatto (salvo richiesta).
