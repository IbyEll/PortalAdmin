---
description: Veve MCP standalone `veve {KEY}` — non usare su gogo/procedi (vedi ADMIN-Veve.db.mdc)
alwaysApply: false
---

# Veve — `veve {KEY}` e workflow step 3

Sequenza: **Leggi Jira** → **Analizza repo** → **Aggiorna Story** → **Aggiorna Subtask**.

| Step veve | Regola |
| --- | --- |
| 1 | `ADMIN-Veve-LeggiJira.mdc` |
| 2 | `ADMIN-AnalizzaRepo.mdc` |
| 3 | `ADMIN-Veve-AggiornaStory.mdc` · `.cursor/templates/veve/veve-story-parent.md` |
| 4 | `ADMIN-Veve-AggiornaSubtask.mdc` · `.cursor/templates/veve/veve-subtask.md` |

## Trigger

| Contesto | Comportamento |
| --- | --- |
| `veve {KEY}` | Standalone — chat libera, sequenza completa |
| `procedi` / `gogo {KEY}` | **Non** applicare — usare `ADMIN-Veve.db.mdc` (step 3 workflow database) |
| `chiudi` | **Non** esegue veve |

`{KEY}` = `{JIRA_KEY}-{num}` del progetto istanziato (`ADMIN-96`, `JLO-507`, …).

Variante **DB** (cache + WIP): `ADMIN-Veve.db.mdc` · `veve db {KEY}` · step 3 di `ADMIN-Workflow database.mdc`.

**MCP veve:** `CallMcpTool` `user-atlassian` — **non** Task Jira Auto.

## Runtime programmatico (server / CI)

Stessa sequenza 1→4 di questa regola, **senza** agente Cursor né MCP — per script, CI e pagina Avanzamento.

| Contesto | Entrypoint |
| --- | --- |
| Jira REST | `admin.portal.JiraCORE/jiraCORE.jira.live.mjs` (non `CallMcpTool`) |

**Vietato:** branch, commit, codice, transizioni Fatto (salvo richiesta esplicita).
