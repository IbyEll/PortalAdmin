# PortalAdmin — Dashboard + cruscotto JLO/ADMIN
# Copia questo file in .env e compila i valori.

# ── Server cruscotto ─────────────────────────────────────────────
DASHBOARD_PORT=3999

# ── Product repo — ADMIN-90 ───────────────────────────────────────
# Default: sibling ../{PRJ_REPO} da overlay config_project attivo (default JustLastOne)
# Nuovo product: lib/config_project.{PRJ_NAME}.mjs
#   + runner/runner.config.{PRJ_NAME}.mjs
#   + lib/config.discovery.{PRJ_NAME}.mjs
PRODUCT_REPO_PATH=

# Overlay product — config.project + runner.config.stack (default JustLastOne se omesso)
# PRJ_NAME=JustLastOne

# ── Servizi stack (health check tab Servizi + runner test) ───────
AUTH_URL=http://localhost:4001/api/v1
API_URL=http://localhost:4000/api/v1
WEB_BASE=http://localhost:3000

# ── Cruscotto SQLite (ADMIN-81 / ADMIN-99) ─────────────────────
# Default: {PortalAdmin}/data/cruscotto.db
CRUSCOTTO_DB_PATH=

# ── Jira ─────────────────────────────────────────────────────────
# API token: https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_EMAIL=
JIRA_API_TOKEN=
JIRA_CLOUD_ID=3caddd74-469e-4ca3-adf8-926f79c98e7c
JIRA_BOARD_ID=68
JIRA_SITE=myfuturejobsearch.atlassian.net
